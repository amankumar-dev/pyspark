# The entire program we submit to sparks knowns as spark application

# spark-submit myscript.py  ( runn this in cmd to submit spark program but when we click on code runner btn it automatically called)